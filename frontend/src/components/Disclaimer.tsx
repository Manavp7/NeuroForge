export default function Disclaimer() {
  return (
    <div className="disclaimer" role="alert">
      ⚠️ RESEARCH / SIMULATION ONLY — synthetic data &amp; surrogate models. NOT a medical device,
      NOT validated, NOT for any clinical, diagnostic, or treatment use.
    </div>
  );
}
